A simple starter repository for network packet manipulation

installing scapy using "apt install python3-scapy"

sniffing a packet for the first time using the sniff() and ping (website) on a new terminal window
